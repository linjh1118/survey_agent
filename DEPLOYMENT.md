# GitHub Pages 部署指南

## 🚀 快速部署

### 1. Fork并配置仓库

1. **Fork此仓库** 到你的GitHub账号
2. **重命名仓库**（可选）：进入Settings → General，修改Repository name
3. **更新README中的链接**：将`your-username`替换为你的GitHub用户名

### 2. 启用GitHub Pages

1. 进入仓库的 **Settings** 页面
2. 滚动到 **Pages** 部分
3. 在 **Source** 下拉菜单中选择 **GitHub Actions**
4. 保存设置

### 3. 触发部署

推送任何代码到main分支，或者：
1. 进入 **Actions** 标签页
2. 选择 **Deploy static content to Pages**
3. 点击 **Run workflow**

### 4. 访问网站

部署成功后，访问：
```
https://your-username.github.io/survey_agent
```

## 🔧 常见问题解决

### 问题1: "Not Found" 错误

**原因**: GitHub Pages未正确启用或配置

**解决方案**:
1. 确保在Settings → Pages中选择了"GitHub Actions"
2. 检查Actions权限：Settings → Actions → General → Workflow permissions
3. 确保选择了"Read and write permissions"

### 问题2: Actions失败

**原因**: 权限不足或文件路径问题

**解决方案**:
1. 检查`.github/workflows/static.yml`文件是否存在
2. 确保仓库有Actions权限
3. 查看Actions日志了解具体错误

### 问题3: 页面显示但功能异常

**原因**: 静态资源加载问题

**解决方案**:
1. 确保所有HTML文件都在根目录
2. 检查浏览器控制台的错误信息
3. 确保`.nojekyll`文件存在

## 📁 文件结构说明

```
survey_agent/
├── .github/workflows/static.yml  # GitHub Actions部署配置
├── .nojekyll                     # 禁用Jekyll处理
├── index.html                    # 主页面
├── papers.html                   # 论文浏览器
├── cache/demo_papers.json        # 演示数据
├── _config.yml                   # GitHub Pages配置
└── README.md                     # 项目说明
```

## 🎯 自定义配置

### 更新演示数据

编辑 `cache/demo_papers.json` 文件来更新论文演示数据：

```json
{
  "arxiv_id": {
    "arxiv_id": "2025.12345",
    "title": "Your Paper Title",
    "summary": "Paper summary here...",
    "cached_at": "2025-11-09T10:00:00.000000",
    "content_hash": "hash_here",
    "model_info": {
      "provider": "your_provider",
      "model_name": "your_model"
    }
  }
}
```

### 更新GitHub链接

在以下文件中将`your-username`替换为你的GitHub用户名：
- `index.html` (页面底部GitHub链接)
- `papers.html` (导航链接)
- `README.md` (徽章和链接)

### 自定义样式

所有样式都内嵌在HTML文件中，可以直接修改CSS变量：

```css
:root {
    --primary-color: #2563eb;
    --primary-dark: #1e40af;
    /* 其他颜色变量 */
}
```

## 🔒 安全注意事项

1. **不要提交敏感信息**：API密钥等敏感信息不要提交到仓库
2. **使用演示数据**：GitHub Pages版本仅用于展示，实际功能需要本地运行
3. **定期更新**：保持依赖和配置文件更新

## 📞 获取支持

如果遇到问题：

1. 检查GitHub Actions日志
2. 查看浏览器开发者工具控制台
3. 参考GitHub Pages官方文档
4. 在仓库中开启Issue寻求帮助

---

部署成功后，你将拥有一个专业的Survey Agent演示网站！🎉